Django==6.0.2
djangorestframework==3.16.1
django-cors-headers==4.6.0
PyJWT[crypto]==2.10.1
certifi==2026.6.17
uvicorn==0.38.0
# прод-сервер приложения; настройки воркеров — backend/gunicorn.conf.py
gunicorn==26.0.0
# раздача статики админки самим приложением в проде (settings.prod)
whitenoise==6.12.0
python-dotenv==1.2.1
openai==2.24.0
anthropic>=0.69
# HTTP-клиент SDK провайдеров. Закреплён явно, потому что через него идёт прокси
# (apps/ai/providers/_clients.py), а параметр `proxy=` появился только в 0.28.
# Экстра socks — для схемы socks5:// в LLM_PROXY_URL.
httpx[socks]==0.28.1
langchain==1.2.10
langchain-openai==1.1.10
langchain-anthropic==1.3.4
langgraph==1.0.9
tiktoken==0.12.0
yandex-ai-studio-sdk==0.19.2
yandexcloud==0.379.0
pgvector==0.4.1
psycopg[binary]==3.2.12
# общий кэш воркеров: лок генерации, rate limit, «Стоп» (settings.CACHES).
# Клиент для django.core.cache.backends.redis — отдельный django-redis не нужен.
redis==8.1.0

# извлечение текста из прикреплённых файлов (apps/conversations/attachments.py)
pypdf==6.1.1
python-docx==1.2.0
